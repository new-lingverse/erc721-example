{
  "name": "erc721-example",
  "scripts": {
    "contracts:test": "turbo test --filter contracts",
    "contracts:deploy": "turbo run deploy --filter contracts",
    "contracts:mint": "turbo run mint --filter contracts"
  },
  "packageManager": "bun@1.11.23",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.0.12"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
